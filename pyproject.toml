[dependencies]
pydantic = "1.9.0"
requests = "2.27.1"

[dev-dependencies]
yapf = "0.32.0"

[build-system]
requires = [
    "setuptools >= 42.0.0",
    "wheel"
]
build-backend = "setuptools.build_meta"
